<div class="stage" id="saved">

    {{#if articles}}
        <div class="article-item shadow">
            <h3 class="article-title">Title</h3>
            <a href="#">Article Links</a>
            <p class="acticle-summary">Summary</p>
        </div>
        {{else}}
        <div class="empty_list">
            <p>it looks like there is nothing saved</p>
        </div>

    {{/if}}
</div>

